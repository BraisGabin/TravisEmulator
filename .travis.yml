language: android

dist: bionic

install:
  - . travis-bionic-android.sh

script:
  - echo $ANDROID_HOME
  - echo $PATH
  - ./gradlew tasks
  - ./gradlew test

after_failure:
  - docker ps -a
  - docker logs device

notifications:
  email: false
